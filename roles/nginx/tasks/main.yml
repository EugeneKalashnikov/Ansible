--- 
- name: Create container for Nginx
  docker_container:
    name: "Nginx-b"
    ports:
    - "8888:80"
    interactive: yes
    image: ubuntu
  become: yes
- name: Update packages
  shell: docker exec Nginx-b apt-get update -y
- name: Install Nginx
  shell: docker exec Nginx-b apt-get install nginx -y && service nginx start
- name: Copying template
  template: src=default.j2 dest=~/DEFAULT
- name: Restarting service Nginx
  shell: docker exec Nginx-b service nginx restart
- name: Check Nginx
  shell: curl 172.17.0.10
